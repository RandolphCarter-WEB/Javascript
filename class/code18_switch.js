var dump = 2;

switch(dump) {
    case 1:
        console.log(`${dump} is 1.`);
        break;
    case 2:
        console.log(`${dump} is 2.`);
        break;
    default:
        console.log("there is notthing number in here");
}